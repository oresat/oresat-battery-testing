chargers_backend.py
In the battery chargers section, does not deal with labjack at all. 
Jobs:
A) Hold the data required for charging batteries: ChargeProfile, Charger Serial Paths
B) Hold the functions required for charging batteries: start_charging(), stop_charging(), define_charger_serial_paths()
Issues: Likely SOLVED--> libb6 did stuff for us -  it's possible that we need a set_ChargeProfile() function since we have it in the original code, but given that we already know we're using lithium ions only I think we can get away with having the ChargeProfile generally behave like a constant since there is only one --> so instead of that line "chargeProfile = libb6.Device.getDefaultChargeProfile()" we can literally just say this_battery_charge_profile = ChargeProfile where ChargeProfile is the dataclass that holds said values
[!!!]What is the command to actually start the charger charging?
---Might need a "stop_charging()" function
Don't worry about UNK1 for now, just send it and don't worry too much about checking packet size.

Viable Commands to send to the chargers: https://github.com/maciek134/libb6/blob/master/Enum.hh
From an enum class:
    GET_DEV_INFO = 0x57,
    GET_SYS_INFO = 0x5a,
    GET_CHARGE_INFO = 0x55,
    UNK1 = 0x5f 
    STOP_CHARGING  = 0xfe,

SEE ME HERE!
Alright, here is how to proceed:
Start by packaging UNK1 and sending it with endpoint_write (package with python struct)
Then get a return value and if it is 4, stop charging (requires packaging that cmd and sending)
Then package all of the profile data and then send the checksum followed by the actual package
    this should start_charging

Note that the checksum is created by summing all of the byte array except for the first 2 bytes
  
_____TESTED, SHOULD BE GOOD
labjack_backend.py 
In the labjack battery data-getting section, does not interact with chargers at all.
Jobs:
A) Hold data required for labjack commands: PINS (DONE)
	i. Have notes about each of the functions (or at least a mention) of the functions we're using such as getAIN() (EH)
B) Hold dataclass for collecting temperature and voltage data: class BankData (DONE)
C) Hold calculation/conversion functions for getting temps and volts: get_actual_temp(), get_data() (DONE)
D) A general STOP() function for the labjack (DONE)
----NOTE: Not sure where to put this yet but the scribe function that appends to the csv file should take the output of the
get_data_from_one_cell function as its input DONE

_____ COMPLETE!
scribe_backend.py
In the labjack battery data-getting section, does not interact with anything.
Jobs:
A) Hold functions for writing to csv file, including timestamps.

_____
main.py (or something similar)
This is the general frontend that combines all parts and holds a class with functions to charge, discharge, test, etc.
A) Select a battery bank/battery and do any or all of the following:
	i. Charge.
	ii. Discharge.
	iii. Get voltage and temperature. Write to csv with timestamp.
	
_____
Thereafter, anyone should be able to easily write any number of testing scripts to fulfill NASA requirements.
The ideal scenario is that this is in a good enough state that anyone can take it and run with it.

